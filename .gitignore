node_modules/
.env
.director-key
house.grant.jsonl
.master-key

# deploy secrets (never commit)
deploy/.env
luke/.env
luke/.luke-key

# app sites synced onto the box by deploy/sites.sh (not part of this repo)
deploy/sites/

# macOS Finder droppings — never part of the site.
.DS_Store
