# Copy to .env (gitignored). The noir-gm service reads this at startup;
# real environment variables override these values. NEVER commit .env.
ANTHROPIC_API_KEY=sk-ant-...
REPLICATE_API_TOKEN=r8_...
# Optional overrides:
# NOIR_MODEL=claude-sonnet-5
# NOIR_IMAGE_MODEL=black-forest-labs/flux-schnell
# NOIR_GM_PORT=8787
